from .calc import Calculator
from .calc import hello_world
# first job of __init__ is tell python that this directory is a package and second job is to import the module in the package so that we can import the module directly from the package without specifying the module name.
#__init__ is creating a linkage between the module and the package. It allows us to import the module directly from the package without specifying the module name.